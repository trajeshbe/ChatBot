# Minimal Fine-Tuning Dependencies for Docker Image
# Extends chatbot-agent-runtime:llm-enabled with only essential fine-tuning libraries
# Date: 2025-12-18
# Updated: Added DPO and GRPO support

# Core PEFT dependencies (only install what's missing from agent-runtime)
peft>=0.7.0
accelerate>=0.24.0
bitsandbytes>=0.41.0
trl>=0.7.0  # Includes DPO trainer

# Ensure compatible versions (don't conflict with existing packages)
transformers>=4.36.0
datasets>=2.15.0

# Additional training utilities
scipy>=1.11.0

# RLHF and Preference Optimization (DPO, GRPO, PPO)
# trl already includes DPOTrainer, no additional packages needed

# Optional: Experiment tracking (lightweight versions)
# tensorboard  # Already in agent-runtime
# wandb  # Optional
